sudo: required
services:
  - docker

before_install:
  - docker build -t trungtin/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run trungtin/react-test npm test -- --coverage

after_success:
  - docker build -t trungtin/multi-client ./client
  - docker build -t trungtin/multi-nginx ./nginx
  - docker build -t trungtin/multi-server ./server
  - docker build -t trungtin/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push trungtin/multi-client
  - docker push trungtin/multi-nginx
  - docker push trungtin/multi-server
  - docker push trungtin/multi-worker
